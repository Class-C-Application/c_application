#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main()
{
	char s[100];
	// 표준 입력으로 한 행(100자 이내)을 문자 배열 s에 저장하세요 (gets_s 사용)

	// char 포인터 p를 s로 초기화하세요

	// p가 가리키는 문자가 널이 아닌 동안, 각 문자를 printf로 출력하고 포인터를 이동하세요

	printf("\n");

	return 0;
}
