#include <stdio.h>

#include <stdlib.h> //rand(), srand()를 위한 헤더파일 포함
#include <time.h>   //time()을 위한 헤더파일 포함

// MAX라는 이름으로 정수 100을 나타내는 매크로 상수를 정의하세요

int main(void)
{
	// srand( (long) time(NULL) ); 을 호출하여 시드를 설정하세요

	// printf("1 ~ %5d 사이의 난수 8개:\n", MAX); 형식으로 출력하세요
	// 0부터 8 미만까지 반복하는 for문으로 printf("%5d ", rand() % MAX + 1); 형식으로 1~MAX 범위의 난수를 출력하세요
	puts("");

	return 0;
}
