#include <stdio.h>
#include <stdlib.h> //rand()위한 헤더파일 포함

int main(void)
{
	// printf("0 ~ %5d 사이의 난수 8개: rand()\n", RAND_MAX); 형식으로 출력하세요
	// 0부터 8 미만까지 반복하는 for문으로 printf("%5d ", rand()); 형식으로 난수를 출력하세요
	puts("");

	return 0;
}
