#include <stdio.h>

// int add(int a, int b); 함수원형을 선언하세요
// int findMax(int, int); 함수원형을 선언하세요
// void printMin(int, int); 함수원형을 선언하세요

int main(void)
{
	// int형 변수 a에 10, b에 15를 선언 및 초기화하세요

	// findMax(a, b)를 호출하여 결과를 int형 변수 max에 저장하세요
	// printf("최대: %d\n", max); 형식으로 출력하세요
	// printf("합: %d\n", add(a, b)); 형식으로 출력하세요

	//반환 값이 없는 함수호출은 일반문장처럼 사용
	// printMin(a, b)를 호출하세요

	return 0;
}

// void printMin(int a, int b) 함수를 정의하세요
// 조건 연산자로 두 값 중 작은 값을 구하고, printf("최소: %d\n", min); 형식으로 출력하세요
