#include <stdio.h>

// void increment(int ary[], int n, int SIZE); 함수원형을 선언하세요
// void printary(int data[], int SIZE); 함수원형을 선언하세요

int main(void)
{
	// int형 배열 data를 { 4, 7, 2, 3, 5 }로 초기화하세요
	// sizeof(data) / sizeof(int)로 배열 길이를 int형 변수 aryLength에 저장하세요
	// int형 변수 inc에 3을 저장하세요

	// printary(data, aryLength)를 호출하여 배열을 출력하세요
	//배열 원소를 모두 3씩 증가시키기 위해 increment() 함수호출
	// increment(data, inc, aryLength)를 호출하세요
	// printf("배열 원소에 각각 %d의 더한 결과: \n", inc); 형식으로 출력하세요
	// printary(data, aryLength)를 호출하여 결과를 출력하세요

	return 0;
}

//배열크기가 SIZE인 배열 ary의 모든 원소를 n만큼 증가시키는 함수
// void increment(int ary[], int n, int SIZE) 함수를 정의하세요
// 0부터 SIZE 미만까지 반복하며 ary[i] += n; 으로 증가시키세요

//배열크기가 SIZE인 배열 ary의 모든 원소를 출력하는 함수
// void printary(int data[], int SIZE) 함수를 정의하세요
// 0부터 SIZE 미만까지 반복하며 printf("%2d ", data[i]); 형식으로 출력하세요
// 마지막에 printf("\n"); 으로 줄바꿈하세요
