#include <stdio.h>

//정적 전역변수 선언
// static 키워드를 사용해 정수형 전역변수 svar를 선언하세요
//전역변수 선언
// 정수형 전역변수 gvar를 선언하세요

//함수 원형
void increment();
void setgvar(int);
//void teststatic();

int main(void)
{
	// count를 1로 초기화해 count <= 5 동안 count를 증가시키는 for문을 작성하고
	//   각 반복에서 increment()를 호출하세요
	// printf("함수 increment()가 총 %d번 호출되었습니다.\n", svar); 형식으로 출력하세요

	// setgvar(30)을 호출하세요
	// printf("전역 변수: %d\n", gvar); 형식으로 gvar를 출력하세요
	//teststatic();
}

//함수 구현
void increment()
{
	// svar를 1 증가시키세요

	// 정수형 지역변수 data에 3을 대입하세요
	// static int value를 data로 초기화하는 정적 지역변수를 선언하세요
}
