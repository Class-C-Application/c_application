#include <stdio.h>

double getArea(double);
double getCircum(double);

//전역변수 선언
// double형 전역변수 PI를 3.14로 선언하세요
// int형 전역변수 gi를 선언하세요

int main(void)
{
	//지역변수 선언
	// double형 지역변수 r을 5.87로 선언하세요
	//전역변수 PI와 같은 이름의 지역변수 선언
	// const double형 지역변수 PI를 3.141592로 선언하세요

	// printf("면적: %.2f\n", getArea(r)); 형식으로 면적을 출력하세요
	// printf("둘레1: %.2f\n", 2 * PI * r); 형식으로 둘레1을 출력하세요
	// printf("둘레2: %.2f\n", getCircum(r)); 형식으로 둘레2를 출력하세요
	// printf("PI: %f\n", PI); 형식으로 지역변수 PI를 출력하세요
	// printf("gi: %d\n", gi); 형식으로 전역변수 gi의 기본값을 출력하세요

	return 0;
}

double getArea(double r)
{
	// 전역변수 PI를 참조해 r * r * PI 값을 반환하세요
}
