#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main(void)
{
	//레지스터 지역변수 선언
	// register 키워드를 사용해 정수형 변수 sum을 0으로 선언하세요

	//메모리에 저장되는 일반 지역변수 선언
	// 정수형 변수 max를 선언하세요
	// printf("양의 정수 입력 >> "); 로 입력 안내를 출력하고
	// scanf를 사용해 max를 입력받으세요

	//레지스터 블록 지역변수 선언
	// register int count를 1로 초기화해 count <= max 동안 count를 증가시키는 for문을 작성하고
	//   각 반복에서 sum += count 연산을 수행하세요

	// printf("합: %d\n", sum); 형식으로 합을 출력하세요

	return 0;
}
