#include <stdio.h>

int main(void)
{
    // 1. 일반 변수 선언 및 데이터 초기화
    int i = 100;
    char c = 'A';

    // 2. [선언부] 각 변수의 주소를 저장할 포인터를 선언하고 초기화하세요.
    [        ] pi = [    ]; // 가이드: int형 포인터 pi를 선언하고 i의 주소(&i)를 대입
    [         ] pc = [    ]; // 가이드: char형 포인터 pc를 선언하고 c의 주소(&c)를 대입

    // 3. [출력부] 포인터를 이용한 간접 참조(Dereference) 출력
    // 가이드: pi와 pc가 가리키는 실제 값을 %d와 %c로 출력하세요.
    printf("간접참조 출력: %d %c\n", [     ], [     ]);

    // 4. [수정부] 포인터를 통해 원본 변수의 값을 수정하세요.
    [      ] = 200; // 가이드: pi를 사용하여 변수 i의 값을 200으로 변경
    [      ] = 'B'; // 가이드: pc를 사용하여 변수 c의 값을 'B'로 변경

    printf("직접참조 출력: %d %c\n", i, c);

    // 5. [재할당] 포인터가 가리키는 대상을 새로운 변수로 바꾸세요.
    int data = 1000;
    [     ] = [      ]; // 가이드: 포인터 pi가 이제 변수 data의 주소를 가리키게 하세요.

    // 6. [확인부] 바뀐 포인터가 가리키는 값을 출력하세요.
    printf("직접참조 출력: %d %d\n", data, [     ]);

    return 0;
}