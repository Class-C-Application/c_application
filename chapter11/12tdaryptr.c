#include <stdio.h>

int main(void)
{
    // 1. 2행 4열 int형 배열 ary 초기화 및 배열 포인터 선언
    int ary[][4] = { {10, 20, 30, 40}, {50, 60, 70, 80} };
    int(*ptr)[4] = ary; // 4개짜리 정수 배열을 가리키는 포인터

    // 2. 배열과 포인터 변수 자체의 크기 비교
    printf("%zd %zd\n", sizeof([     ]), sizeof([     ])); // 가이드: 배열 전체 크기 vs 포인터 변수 자체의 크기
    printf("%zd %zd\n\n", sizeof([        ]), sizeof([        ])); // 가이드: 첫 번째 행(ary[0])의 크기 vs 포인터가 가리키는 대상(ptr[0])의 크기

    // 3. 이중 역참조를 이용한 첫 번째 원소 출력 (10)
    printf("%2d %2d\n", [      ], [      ]); // 가이드: ary와 ptr을 이중 역참조(**)

    // 4. 두 번째 행의 첫 번째 원소 접근 (50)
    printf("%2d %2d\n", [            ], [          ]); // 가이드: (ary+1) 이중 역참조 vs ary[1] 단일 역참조

    // 5. 배열 포인터를 이용한 두 번째 행 접근 (50)
    printf("%2d %2d\n", [            ], [          ]); // 가이드: (ptr+1) 이중 역참조 vs ptr[1] 단일 역참조

    // 6. 두 번째 행의 두 번째 원소 접근 (60)
    printf("%2d %2d\n", [              ], [              ]); // 가이드: ary[1]에서 +1 이동 후 역참조 vs ptr[1]에서 +1 이동 후 역참조

    // 7. 두 번째 행의 마지막(네 번째) 원소 접근 (80)
    printf("%2d %2d\n\n", [                  ], [                  ]); // 가이드: (ary+1) 행 이동 후 +3 열 이동하여 역참조

    // 8. 포인터 증감 연산과 출력 (포인터가 가리키는 '행'이 이동함)
    printf("%2d ", [          ]); // 가이드: 현재 ptr의 값을 출력하고 ptr을 다음 행으로 증가(ptr++)
    printf("%2d\n", [       ]);   // 가이드: 증가된 ptr이 가리키는 행의 첫 번째 값 출력

    return 0;
}