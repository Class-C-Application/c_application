#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

#define SIZE 3

int main(void)
{
	// int형 포인터 배열 pary[SIZE]를 {NULL}로 초기화하세요
	// int형 변수 a=10, b=20, c=30을 선언하세요
	// pary[0]=&a, pary[1]=&b, pary[2]=&c를 대입하세요
	// for문으로 *pary[i]를 출력하세요: printf("*pary[%d] = %d\n", i, *pary[i])

	// for문으로 pary[i]를 scanf로 입력받고 a, b, c를 출력하세요
	//   printf("%d, %d, %d\n", a, b, c)

	return 0;
}
