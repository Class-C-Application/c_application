#include <stdio.h>

int main(void)
{
    // 1. 초기화
    int a[] = {10, 20};
    int *p = [      ]; // 가이드: 배열 a의 첫 번째 원소 주소(&a[0])를 대입하세요

    // 2. 포인터 연산의 기초
    // 가이드: 현재 주소(p), 현재 값(*p), 다음 주소(p+1), 다음 값(*(p+1))을 출력하세요
    printf("%p %d %p %d\n\n", [  ], [    ], [      ], [         ]);

    // 3. *p++ (후위 증감 포인터 연산)
    // 가이드: 현재 p가 가리키는 값을 먼저 반환한 뒤, p의 '주소'를 다음으로 이동시킵니다.
    printf("%d\n", [       ]); 
    printf("%p %d\n\n", p, *p); // 결과 확인: p는 &a[1]로 이동해 있을 것입니다.

    // 4. *++p (전위 증감 포인터 연산)
    p = &a[0]; // 가이드: p를 다시 첫 번째 주소로 초기화하세요
    // 가이드: p의 '주소'를 먼저 다음으로 이동시킨 뒤, 그 위치의 값을 반환합니다.
    printf("%d\n", [       ]); 
    printf("%p %d\n\n", p, *p); // 결과 확인: p는 &a[1]이고, 값은 20이 출력됩니다.

    // 5. (*p)++ (후위 증감 값 연산)
    p = &a[0]; // 가이드: p를 다시 초기화하세요
    // 가이드: p가 가리키는 '값'을 먼저 반환한 뒤, 그 '값' 자체를 1 증가시킵니다. (주소 이동 없음)
    printf("%d\n", [       ]); 
    printf("%p %d\n\n", p, *p); // 결과 확인: p는 여전히 &a[0]이지만, a[0]의 값은 11이 됩니다.

    // 6. ++*p (전위 증감 값 연산)
    a[0] = 10;
    p = &a[0]; // 초기화
    // 가이드: p가 가리키는 '값'을 먼저 1 증가시킨 뒤, 그 결과를 반환합니다. (주소 이동 없음)
    printf("%d\n", [       ]); 
    printf("%p %d\n\n", p, *p); // 결과 확인: p는 &a[0]이고, 값은 11이 출력됩니다.

    return 0;
}