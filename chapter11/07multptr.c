#include <stdio.h>

int main(void)
{
    // 1. [선언부] 변수, 포인터, 이중 포인터를 계층적으로 연결하세요.
    int i = 100;
    [         ] pi = [    ];  // 가이드: int형 포인터 pi 선언 및 i의 주소 대입
    [         ] dpi = [    ]; // 가이드: int형 이중 포인터 dpi 선언 및 pi의 주소 대입

    // 2. [주소 확인] 여러 가지 표현으로 i의 주소를 출력해 보세요.
    // 가이드: &i, pi, *dpi는 모두 같은 주소값을 가집니다.
    printf("%p %p %p\n", [    ], [     ], [      ]);

    // 3. [수정부 1] 싱글 포인터(pi)를 이용해 i의 값을 130으로 만드세요.
    [      ] = i + 30; 
    // 가이드: i, *pi, **dpi 세 가지 방식으로 수정된 값을 확인하세요.
    printf("%d %d %d\n", [   ], [     ], [       ]);

    // 4. [수정부 2] 이중 포인터(dpi)를 이용해 i의 값을 160으로 만드세요.
    [        ] = *pi + 30; 
    // 가이드: 최종적으로 수정된 값을 다시 한번 세 가지 방식으로 확인하세요.
    printf("%d %d %d\n", [   ], [     ], [       ]);

    return 0;
}