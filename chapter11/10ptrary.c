#include <stdio.h>

int main(void)
{
    // 1. 배열 이름은 그 자체로 첫 번째 원소의 주소입니다.
    int score[] = {10, 20, 30};
    // 가이드: 배열 이름 score와 다음 원소의 주소(score + 1)를 출력해 보세요.
    printf("%p %p\n", [      ], [          ]);
    // 가이드: 역참조(*)를 사용하여 10과 20을 출력하세요.
    printf("%d %d\n\n", [       ], [           ]);

    // 2. 포인터 변수를 이용한 배열 접근
    int a[3] = {5, 10, 15};
    [        ] p = a; // 가이드: 배열 a를 가리킬 정수형 포인터 p를 선언하세요.

    // 가이드: 포인터 연산(p+i)과 배열 인덱스(p[i]) 두 가지 방식으로 모든 원소를 출력하세요.
    printf("%d %d %d\n", *(p), [         ], [         ]);
    printf("%d %d %d\n", p[0], [      ], [      ]);

    // 3. 포인터 증감 연산 (주소 이동)
    // 가이드: 현재 값을 출력한 뒤 p를 다음 칸으로 옮기세요. (결과: 5 출력)
    printf("%d ", [       ]); 
    printf("%d\n\n", *p); // 가이드: 이동한 위치의 값(10)을 확인하세요.

    // 4. 포인터 이동과 값의 변경 혼합
    p = &a[2]; // p가 마지막 원소(15)를 가리키게 초기화
    // 가이드: 현재 값(15) 출력 후 p를 이전 칸으로 옮기세요.
    printf("%d ", [       ]); 

    // 가이드: 현재 값(10)을 출력한 뒤, 그 '값'을 1 감소시키세요. (결과: 10 출력, 메모리엔 9 저장)
    printf("%d\n", [         ]); 

    // 5. 상대적 위치 확인 (현재 p는 a[1]을 가리키며 값은 9인 상태)
    // 가이드: p를 기준으로 앞의 칸, 현재 칸, 뒤의 칸의 값을 출력하세요.
    printf("%d %d %d\n", *(p - 1), [     ], [         ]);
    printf("%d %d %d\n", p[-1], [      ], [      ]);

    return 0;
}