#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main(void)
{
    // printf("양의 정수 입력 >> ");
    // printf("\nfor 문으로 구한 1에서 %d까지 합: %3d\n", 변수1, 변수2);
    // printf("while 문으로 구한 1에서 %d까지 합: %3d\n", 변수1, 변수2);
    return 0;
}
